<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ron.dojotraining">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DojoTraining">
        <activity android:name=".EyeLastpoint"></activity>
        <activity android:name=".RoomGtoeye" />
        <activity android:name=".RoomFtoEye" />
        <activity android:name=".RoomEtoEye" />
        <activity android:name=".Efg" />
        <activity android:name=".Ddactivty" />
        <activity android:name=".CcActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>